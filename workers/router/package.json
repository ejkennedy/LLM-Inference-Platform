{
  "name": "@llm-inference-platform/router",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@llm-inference-platform/types": "file:../../shared/types"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^3.0.0"
  }
}
